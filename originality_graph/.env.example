# Optional GitHub token. The graph itself comes from deps.dev (no auth needed),
# so this solution produces a strong, well-spread ranking even without a token.
# A token only helps if you extend the graph with GitHub-derived signals.
GITHUB_TOKEN=
